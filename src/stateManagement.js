// import createPersistedState from 'use-persisted-state';
// const useLightModeState = createPersistedState('modeNumber');

// const useLightMode = (initialMode) => {
//   const [modeNumber, setLightMode] = useLightModeState(initialMode);

//   return {
//     modeNumber,
//     toggle: () =>
//       setLightMode((currentModeNumber) => {
//         if (currentModeNumber === 0) {
//           currentModeNumber = 1;
//         } else {
//           currentModeNumber = 0;
//         }
//         console.log('toggled in hook!');
//       }),
//   };
// };

// export default useLightMode;
